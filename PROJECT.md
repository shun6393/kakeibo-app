# 家計簿アプリ PROJECT

## 1. 文書の位置づけ

この文書は、自分専用の家計簿Webアプリにおける確定仕様の正本とする。

- アプリの要件と画面設計は、ユーザーとChatGPTで検討して決定する。
- 実装はCodexが担当し、実装・保守・データ設計上の改善案も提示する。
- 改善案は確定仕様と区別し、再検討後に採用する。
- v0.1では小さく完成させ、将来の拡張性を残しつつ過剰設計を避ける。

## 2. アプリの目的

バイト収入、日常の支出、ゲーム支出、サブスクリプションを手動で管理する、1人用のシンプルな家計簿Webアプリを作る。

主に確認する情報は次のとおり。

- 現在の残高
- 今月の収入・支出・収支
- カテゴリ別の支出
- ゲームごとの支出
- サブスクリプションの支出と次回更新日
- 月間・年間・全期間の集計

## 3. 対象環境と制約

- HTML、CSS、JavaScriptで実装する。
- GitHub Pagesで公開できる静的構成とする。
- ログインおよびサーバーは使用しない。
- データはブラウザのLocalStorageに保存する。
- LocalStorageの固定キー名は `kakeibo-app-data` とし、互換性維持のため将来も変更しない。
- 復元直前の安全退避には固定キー名 `kakeibo-app-data-restore-safety` を使用する。このキーは次回の復元時に上書きし、全データ削除時には通常データとともに削除する。
- 利用者は1人とする。
- スマートフォンを主端末とし、PCにも対応する。
- v0.1では端末間およびブラウザ間の同期を行わない。
- 収支は手動登録する。サブスクリプションのみ更新日に自動で支出を作成する。
- 金額の通貨は日本円のみとし、小数・0円・負数を許可しない。
- モバイルファーストで設計し、初期版では装飾より見やすさと操作の単純さを優先する。
- メインカラーは緑系とする。

## 4. v0.1の機能範囲

### 4.1 初回設定と現在の残高

初回起動時に次を入力する。

- 初期残高
- 残高の基準日

初期残高は「基準日終了時点の現在残高」として入力する。現在の残高は次の式で算出する。

```text
初期残高 + 基準日翌日以降の収入 - 基準日翌日以降の支出
```

残高は口座別・支払い方法別には管理せず、アプリ全体で1つの金額として扱う。

例えば7月23日を基準日として初期設定した場合、7月23日終了時点の残高を入力し、7月24日以降の取引を自動計算対象とする。基準日以前および基準日当日の取引は履歴に存在しても現在残高の計算対象に含めない。

現在残高の計算結果自体は保存せず、表示時に取引データから算出する。計算対象は取引元を限定せず、手動取引と将来追加されるサブスクリプション由来取引を同じ規則で扱う。初期残高が未設定の場合は計算しない。

初期残高と基準日はデータ管理画面から訂正できる。変更前に、基準日の変更によって現在残高の計算対象となる取引範囲が変わることを明示して確認する。変更履歴を取引として追加せず、`balanceBaseline` を更新する。

### 4.2 ホーム画面

次の情報を表示する。

- 現在の残高
- 今月の収入
- 今月の支出
- 今月の収支（今月の収入 - 今月の支出）
- 今月のゲーム支出
- 今月のサブスクリプション支出
- 次回のサブスクリプション更新予定
- 今月の大カテゴリ別支出内訳

今月の支出内訳は集計画面と同じ大カテゴリ別集計、並び順、ID由来の色を使用する。ホーム専用のドーナツグラフ中央に今月の支出合計を表示し、金額・割合一覧は上位5件までとする。「詳しく見る」から集計画面へ移動できるようにする。支出がない場合は空表示とし、Chart.jsを読み込めない場合も一覧と遷移操作は利用可能にする。

今月のサブスクリプション支出は、カテゴリにかかわらず取引の `source` が `subscription` である支出を集計する。次回更新予定は利用中サブスクリプションから最大3件を表示し、`nextBillingDate` の早い順、同日は名前順、同名は登録日時順、それも同じ場合はID順とする。各項目には名前、次回更新日、金額を表示する。対象がない場合は「サブスク未登録」と表示する。「すべて見る」は件数にかかわらず表示し、サブスクリプション管理画面へ移動する。

### 4.3 収支登録

登録項目は次のとおり。

- 種別：収入または支出
- 日付
- 金額
- 内容（任意）
- 大カテゴリ
- 小カテゴリ（存在する場合）
- 支払い方法

メモ欄は実装しない。内容は空文字を許可する。金額は正の整数円のみを受け付け、収入・支出は種別で区別する。

### 4.4 履歴

- 収支の一覧表示
- 月の切り替え
- カテゴリによる絞り込み
- 通常の収支の編集
- 通常の収支の削除
- サブスクリプション由来の支出であることの表示

履歴の初期表示は今月とし、前月・翌月への移動と今月へ戻る操作を提供する。未来日付の手動取引を登録できる仕様と整合させるため、未来月への移動も許可する。

履歴は「すべて・収入・支出」と大カテゴリで絞り込める。種別が収入の場合は収入用大カテゴリ、支出の場合は支出用大カテゴリだけをカテゴリ候補に表示する。

履歴の見出しは、内容が入力されていれば内容を表示する。空文字の場合は小カテゴリ名を、該当する小カテゴリがなければ大カテゴリ名を表示する。

手動取引は、種別、日付、金額、内容、大カテゴリ、小カテゴリ、支払い・受取方法を編集できる。編集時も新規登録と同じ検証を行い、`createdAt` を維持して `updatedAt` だけを更新する。

手動取引の削除前には内容、日付、金額を示して確認する。取引元が `subscription` の取引には手動取引用の削除操作を提供せず、保存処理側でも削除を拒否する。

検索機能は実装しない。サブスクリプション由来の支出は削除不可とする。

サブスクリプション由来の支出で編集できる項目は次に限定する。

- 金額
- 支払い方法
- 大カテゴリ
- 小カテゴリ

次の項目は編集できない。

- 収入／支出種別
- 計上日
- 自動生成元
- サブスクリプションID
- 更新分識別日

### 4.5 カテゴリ

カテゴリは安定した内部IDを持ち、表示名を集計や重複防止の識別子として使用しない。

v0.1では次を可能にする。

- 大カテゴリの追加
- 小カテゴリの追加
- ゲームカテゴリへのゲーム名の追加

編集、削除、並び替えはv0.2以降とする。小カテゴリを持たない大カテゴリも許可し、収支登録時の小カテゴリは任意とする。

- 大カテゴリ追加時は収入用または支出用のどちらか一方を指定し、UUID、同じ種別内の末尾 `order`、`createdAt`、`updatedAt` を設定する。追加直後は小カテゴリ0件とする。
- 小カテゴリ追加時は親カテゴリを指定し、その親の末尾 `order`、UUID、`createdAt`、`updatedAt` を設定する。
- 名称は前後の空白を除去して保存し、1文字以上49文字以内とする。制御文字は使用できない。
- 重複判定では前後空白、Unicodeの表示上の差、英字の大文字小文字を無視する。大カテゴリは同じ収支種別内、小カテゴリは同じ親カテゴリ内で同名を禁止する。
- 追加によって既存項目の `order` は変更しない。

#### 収入カテゴリ

内部構造では「収入」を大カテゴリとし、次の小カテゴリを持たせる。

- 収入
  - バイト給与
  - 臨時収入
  - その他

登録画面では大カテゴリ名の「収入」を表示せず、バイト給与・臨時収入・その他だけを選択肢として表示する。

#### 支出カテゴリ

- ゲーム
  - 原神
  - ドラクエウォーク
  - ゲーム購入
- サブスク
  - ChatGPT
  - Amazon Prime
  - FANBOX
  - Googleフォト
  - iCloud+
  - その他
- 食費
  - コンビニ
  - 外食
  - スーパー
  - カフェ
  - 飲み物
- PC・ガジェット
  - PCパーツ
  - 周辺機器
  - デスク環境
- 勉強
  - 大学
  - 資格
  - その他
- 交通費
  - 電車
  - バス
- 医療
  - 医療
- 買い物
  - 買い物
- その他
  - その他

小カテゴリが1つだけの大カテゴリでは、登録画面に小カテゴリ選択を表示せず、その小カテゴリを自動選択する。

ゲーム支出は、大カテゴリの内部IDが「ゲーム」である支出を対象とする。ゲーム別集計では小カテゴリをゲーム名として扱う。

### 4.6 支払い方法

初期値は次のとおり。

- ちばぎん
- ちばぎんデビット
- PayPay
- PayPal
- 現金
- その他

v0.1では支払い方法を追加できる。編集、削除、並び替えはv0.2以降とする。支払い方法も安定した内部IDを持つ。

追加時はUUID、全支払い方法の末尾 `order`、`createdAt`、`updatedAt` を設定する。名称はカテゴリと同じ入力規則で検証し、前後空白・Unicode正規化・大文字小文字を無視して既存項目と同名になる追加を禁止する。

支払い方法は収入・支出で共通管理する。登録画面の項目名だけを種別に応じて変更する。

- 支出：支払い方法
- 収入：受取方法

### 4.7 サブスクリプション管理

登録項目は次のとおり。

- 名前
- 金額
- 大カテゴリ
- 小カテゴリ
- 支払い方法
- 更新周期
- 次回更新日
- 状態：有効または停止中

更新周期は毎月と毎年のみ対応する。

正式な保存フィールドは `id`、`name`、`amount`、`categoryId`、`subcategoryId`、`paymentMethodId`、`cycle`、`billingDay`、`billingMonth`、`nextBillingDate`、`active`、`stoppedAt`、`createdAt`、`updatedAt` とする。月額の `billingMonth` は `null` とする。

#### 自動計上

- 更新日に支出として自動計上する。
- 長期間アプリを開かなかった場合は、未処理の過去分も順番に自動計上する。
- 同じ更新分は複数回登録しない。
- 金額変更は次回以降の自動計上にのみ反映し、過去履歴は変更しない。
- 停止すると今後の自動計上だけを止め、過去履歴は残す。
- 停止したサブスクリプションは停止中一覧へ移し、再開できる。
- 停止期間中の更新分は計上しない。
- 再開時には新しい次回更新日を利用者が設定する。
- 再開時の次回更新日は端末ローカル日付の今日以降に限定する。
- 停止期間中に過ぎた更新分は再開時にも補完しない。
- サブスクリプション由来の支出は削除できない。
- サブスクリプション由来の支出の編集範囲は履歴の仕様に従う。

#### 日付規則

- 31日など更新日が存在しない月は、その月の末日に計上する。
- 2月29日の年次更新は、平年では2月28日に計上する。
- 日付は端末のローカル日付を使用し、`YYYY-MM-DD` として扱う。
- 月末への一時的な繰り上げ後も本来の基準日を失わないよう、月額では更新基準日を保持する。

#### 二重計上防止

自動生成された取引に最低限、次を記録する。

- `source`: サブスクリプション由来であること
- `subscriptionId`: 元のサブスクリプションID
- `subscriptionOccurrenceDate`: 対象となった更新日

`subscriptionId` と `subscriptionOccurrenceDate` の組み合わせを更新分の一意キーとして扱う。名前、金額、カテゴリなど変更可能な値では重複判定しない。

アプリ起動時は、データ読み込みと検証の完了後に未処理分を計算し、全処理が成功した場合だけ取引と次回更新日をまとめて保存する。

- 1回の処理で確認する更新分は全サブスクリプション合計600件を上限とする。上限を超えた場合は処理全体を中止し、生成途中のデータを保存しない。
- 停止操作の直前にも当日までの自動計上を先にメモリ上で処理し、停止変更とまとめて1回保存する。

### 4.8 集計

期間は次から選択する。

- 今月
- 先月
- 今年
- 全期間

支出を次の軸で集計する。

- 大カテゴリ別
- ゲーム別
- 支払い方法別

Chart.js 4.5.1を固定バージョンのjsDelivr CDNから読み込み、v0.1では中央に対象合計を表示するドーナツ型の円グラフのみ実装する。CDNの読み込みに失敗した場合も、金額・割合一覧は利用可能な状態を保つ。

- 金額の高い順に並べる。
- 12時方向から時計回りに金額の高い順で表示する。
- 凡例とグラフ下の一覧も同じ順序にする。
- 一覧には名称、金額、割合を表示する。
- グラフ、凡例、一覧には同一の並べ替え済み集計データを使用する。
- 金額0円の項目はグラフに表示しない。
- データがない場合は空のグラフではなく案内を表示する。
- 同額の場合はカテゴリ・小カテゴリ・支払い方法の設定順、さらに同順の場合は表示名順で並べる。
- 大カテゴリ別と支払い方法別の対象合計は期間内の全支出、ゲーム別の対象合計は期間内のゲーム支出とする。
- 選択期間内であれば未来日の支出も含め、収入は集計対象外とする。
- 各項目の色は内部IDから決定的に生成し、期間や金額が変わっても同じIDには同じ色を割り当てる。色はAppDataへ保存しない。
- Chart.js内蔵凡例は使わず、モバイルで折り返せるHTML凡例を一覧と同じ集計データから生成する。
- 集計表示のためにスキーマを変更せず、`schemaVersion: 1` の既存データをそのまま利用する。

今月のサブスクリプション支出は、カテゴリ名ではなく取引の `source` を基準に集計する。

### 4.9 データ管理

#### JSONバックアップ

- LocalStorage内のアプリデータをJSONファイルとして出力する。
- JSONにはデータ形式の `schemaVersion` とルート直下の出力日時 `exportedAt` を含める。
- バックアップ対象には設定、カテゴリ、支払い方法、取引、サブスクリプションを含める。
- ファイル名は `kakeibo-backup-YYYY-MM-DD-HHMMSS.json` とする。
- ダウンロード開始後に `settings.lastBackupAt` を更新し、画面へ最終バックアップ日時を表示する。未実施時は「未実施」と表示する。

#### JSON復元

- 復元は現在データとの統合ではなく全置換とする。
- ファイル選択後に検証し、作成日時、形式バージョン、初期残高の有無、取引・サブスクリプション・カテゴリ・支払い方法の件数を確認画面に表示する。
- 復元実行前に、現在データを `kakeibo-app-data-restore-safety` へ退避し、同じ内容のJSONダウンロードを開始してから復元する。
- 安全退避または自動バックアップの開始に失敗した場合は復元を中止する。
- JSONの構文、形式バージョン、必須項目、全レコードのID重複、金額、日付、参照関係、取引元とサブスクリプション情報の整合性を検証する。
- 検証に失敗した場合は現在データを変更しない。
- 対応していない新しい `schemaVersion` のデータは復元しない。
- 復元後はページを再読み込みせず、ホーム、履歴、初期残高、カテゴリ、支払い方法を再描画する。

#### CSV出力

- 収入と支出を含む取引履歴を出力する。
- CSVは閲覧・表計算用とし、復元には使用しない。
- 日付は `YYYY-MM-DD`、金額は数値で出力する。
- 日本語環境を考慮し、UTF-8 BOM付きとする。
- 全取引を日付の古い順、同日は登録日時の古い順で出力する。
- 列は、日付、種別、金額、内容、大カテゴリ、小カテゴリ、支払い・受取方法、登録元、サブスクID、サブスク更新日、登録日時、更新日時とする。

#### 全データ削除

- 削除対象と影響を示す警告の後、確認文字列 `DELETE` の完全一致を求める二段階確認とする。
- 警告画面には先にJSONバックアップを開始できる操作を置く。
- 実行時は通常データと復元前安全退避を削除し、新しい初期データを生成して初期残高設定へ戻す。
- アプリ以外のLocalStorageキーは削除しない。

#### 保存の安全性

- v0.1の初期 `schemaVersion` は `1` とする。
- 初回起動時は初期カテゴリ・支払い方法を含むルートデータを生成し、LocalStorageへ保存する。
- 読み込み時にJSONと基本データ形式を検証し、不正な既存データを自動上書きしない。
- ルートデータの `appMetadata.revision` を保存ごとに1増やし、`updatedAt` を更新する。
- 保存エラーを利用者へ表示する。
- データ全体に更新日時またはリビジョン番号を持たせる。
- 古い形式から移行する前に元データを退避する。
- 複数タブでの更新を検知した場合は再読み込みを促す。
- 設定画面に最終バックアップ日時を表示する。

## 5. 推奨データモデル

LocalStorageにはアプリ全体を1つのルートオブジェクトとして保存する。概念構造は次のとおり。

```text
AppData
├─ schemaVersion
├─ appMetadata
├─ settings
├─ balanceBaseline
├─ categories
├─ paymentMethods
├─ transactions
└─ subscriptions
```

### 5.1 共通方針

- 金額は整数円で保存する。
- 日付のみ必要な値は `YYYY-MM-DD` で保存する。
- 作成・更新日時はISO 8601形式で保存する。
- レコードIDにはUUIDを使用する。
- 初期カテゴリにも固定IDを割り当てる。
- 削除可能になる将来を見越し、取引はカテゴリ名ではなくカテゴリIDを参照する。
- LocalStorageにはルートオブジェクト全体を1件として保存し、保存前に基本形式を検証する。

### 5.2 アプリメタデータ

```text
appName
createdAt
updatedAt
revision
```

`revision` は初期データ作成後の最初の保存を `1` とし、保存成功ごとに1増やす。

### 5.3 概念フィールド

#### 残高基準

```text
amount
date
createdAt
```

初期残高と基準日は後日訂正できる。訂正時は既存の `balanceBaseline` を更新し、別取引は作成しない。

#### 取引

```text
id
type: income | expense
date
amount
title: 空文字可
categoryId
subcategoryId: 任意
paymentMethodId
source: manual | subscription
subscriptionId: 任意
subscriptionOccurrenceDate: 任意
createdAt
updatedAt
```

#### カテゴリ

```text
id
name
transactionType: income | expense
order
createdAt
updatedAt: v0.1ステップ7以降に追加した項目。既存項目では省略可能
subcategories[]
```

#### 支払い方法

```text
id
name
order
createdAt
updatedAt: v0.1ステップ7以降に追加した項目。既存項目では省略可能
```

#### サブスクリプション

```text
id
name
amount
categoryId
subcategoryId: 任意
paymentMethodId
cycle: monthly | yearly
billingMonth: 年額の場合。月額はnull
billingDay
nextBillingDate
active: boolean
stoppedAt: 停止中はISO 8601日時、利用中はnull
createdAt
updatedAt
```

## 6. 画面構成

JavaScriptによるシンプルなシングルページ構成を基本とする。

### スマートフォン

下部ナビゲーションを使用する。

- ホーム
- 履歴
- 登録
- 集計
- その他

「その他」からサブスクリプション管理、カテゴリ・支払い方法の追加、データ管理へ移動する。

### PC

スマートフォンと同じ情報構造を維持し、画面幅に応じて上部またはサイドナビゲーションへ調整する。

## 7. v0.1で実装しないもの

- 銀行、PayPal、PayPayなどの外部API連携
- レシート読み取り
- ログイン
- 複数端末同期
- AIによる自動分類
- 株・投資の管理
- 借金・ローンの管理
- カテゴリの編集、削除、並び替え
- 支払い方法の編集、削除、並び替え
- 検索
- メモ
- 予算管理
- 口座別残高管理
- CSVからの復元
- 円グラフ以外のグラフ

## 8. 実装時に提案・相談する事項

v0.1の基本仕様は確定済みとする。次の事項は仕様を勝手に変更せず、必要になった時点で改善案としてユーザーへ相談する。

1. 追加直後のカテゴリ・支払い方法の誤入力を訂正する手段を設けるか。
2. 復元直前バックアップの保存確認を二段階操作にするか。

## 9. 完了条件

v0.1は、スマートフォンおよびPCで主要操作を完了でき、サブスクリプションを二重計上せず、JSONバックアップから安全に復元できることをもって完成とする。
